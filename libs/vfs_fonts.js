// Placeholder for pdfmake VFS fonts
// This file should contain the font definitions for offline use
// For now, we'll create a minimal stub

if (typeof window !== 'undefined' && window.pdfMake) {
  window.pdfMake.vfs = {};
}