// Placeholder for pdfmake library
// This file should contain the pdfmake library content for offline use
// For now, we'll create a minimal stub to prevent loading errors

if (typeof window !== 'undefined') {
  window.pdfMake = {
    createPdf: function(docDefinition) {
      return {
        open: function() {
          alert('PDF functionality requires the full pdfmake library. Please check CDN connectivity.');
        },
        download: function(filename) {
          alert('PDF functionality requires the full pdfmake library. Please check CDN connectivity.');
        }
      };
    }
  };
}